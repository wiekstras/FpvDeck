# Reproducible UI captures

These images are rendered by the actual Qt/QML simulator with deterministic
showcase data. Regenerate all six at 1280×720 with:

```sh
./scripts/screenshot-demo
```

The FPV image uses generated test bars; no third-party footage is committed.
Hardware state shown in these images is simulated unless explicitly identified.
