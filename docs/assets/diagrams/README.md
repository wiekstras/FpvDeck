# Architecture diagrams

Each architecture view has an editable Mermaid (`.mmd`) source and a rendered SVG
for direct GitHub display. Keep labels and status consistent with the engineering
documents; diagrams must not turn provisional hardware into a confirmed claim.
